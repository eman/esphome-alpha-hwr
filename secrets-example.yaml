# Copy to secrets.yaml and fill in your own values. secrets.yaml is gitignored;
# this template is not, so never put real credentials here.
#
#   cp secrets-example.yaml secrets.yaml
#
# The two security-critical values below are deliberately unusable: `api_key`
# is not a valid key, and `ota_password` is commented out. Copying this file
# and building gives you an error naming the one you have not set yet, which is
# the intended behaviour — a template whose defaults merely *look* like
# placeholders gets flashed as-is, and then the encryption key guarding every
# pump write service is one that anybody can read in this repository.
#
# CI does not use this file. It writes its own throwaway secrets with a freshly
# generated key, precisely so that these placeholders are free to fail.

wifi_ssid: "your-wifi-ssid"
wifi_password: "your-wifi-password"

# Fallback hotspot the node raises when it cannot reach your WiFi.
# ESPHome requires 8-64 characters.
ap_password: "your-ap-password"

# Base64, 32 bytes. Generate your own — `esphome wizard` prints one, or see
#   https://esphome.io/components/api.html#configuration-variables
# The value below is not valid base64 of the right length, so ESPHome will
# reject it until you replace it.
api_key: "REPLACE-ME-WITH-A-GENERATED-KEY"

# Guards OTA firmware upload: without it, anything on your LAN can flash this
# node. Uncomment and set your own.
#
# Note that ESPHome accepts an *empty* password here and silently disables OTA
# authentication entirely, so delete this line rather than blanking it if you
# genuinely want no OTA auth.
#
# ota_password: "your-ota-password"
