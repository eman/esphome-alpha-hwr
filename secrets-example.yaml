# Copy to secrets.yaml and fill in your own values. secrets.yaml is gitignored;
# this template is not, so never put real credentials here.
#
#   cp secrets-example.yaml secrets.yaml
#
# CI also copies this file so `esphome config` can validate the example
# configurations, which reference these keys via !secret.

wifi_ssid: "your-wifi-ssid"
wifi_password: "your-wifi-password"

# Base64, 32 bytes. Generate a real one — do not ship this placeholder:
#   https://esphome.io/components/api.html#configuration-variables
api_key: "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="

ota_password: "change-me"
